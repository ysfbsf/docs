---
title: 'As Tech Partner Configuration Guide'
description: 'This guide outlines the variables that need to be provided for each provider when implementing them for new accounts on the Missena platform.'
icon: 'code'
---

## Xandr

- **Global Variables**:
  - `member_id`: The global member ID for the account.

- **Per Website Variables**:
  - `placement_id`: The placement ID specific to each website (domain/subdomain).

## Eplanning

TODO

## Explorads

TODO

## Improvedigital

- **Global Variables**:
  - `partner_id`: The partner ID for the account (currently set to "2157").

- **Per Website Variables**:
  - `site_id`: The site ID specific to each website. (currently set to "450684" for banner)
  - `exchangetagid`: the exchange tag ID for the website. (currently set to "22920126" for banner)

## Magnite (Rubicon)

- **Global Variables**:
  - `account_id`: The account ID for Magnite (currently set to "24548").

- **Per Website Variables**:
  - `site_id`: The site ID for each website (currently set to "427180").
  - `zone_id`: The zone ID for each website (currently set to "2436742").

- **Authentication**:
  - `username` and `password`: For basic authentication.

## Openweb

TODO

## Pubmatic

- **Global Variables**:
  - `pubId`: The publisher ID (currently set to "162770").

- **Per Website Variables**:
  - `site_id`: The site ID for each website (currently set to "1032340").

## Smart

- **Global Variables**:
  - `callerId`: The caller ID (currently set to "125").

## Smilewanted

TODO

## Triplelift

- **Global Variables**:
  - `supplier_id`: The supplier ID (currently set to "68").

- **Per Request Variables**:
  - `ASI`: The ASI value for supply chain (currently set to "themoneytizer.com").
  - `SID`: The SID value for supply chain (currently set to "2381").

## Criteo

- **Global Variables**:
  - `profile`: The profile ID (currently set to "342").
  - `networkid`: The network ID (currently set to "10593").

- **Per Website Variables**:
  - `uid`: The ad unit ID specific to each website.
